<Window x:Class="CommunityHub.Ui.Views.CitiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Gradovi" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Nazad"
                        Click="BackButton_Click"
                        Padding="15,8"
                        FontSize="14"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Tabela -->
            <Grid Grid.Column="0" Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Popis gradova"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                <DataGrid Grid.Row="1"
                          x:Name="CitiesDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          RowHeaderWidth="0"
                          AlternatingRowBackground="#F2F2F2"
                          BorderBrush="#CCCCCC"
                          BorderThickness="1"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#E0E0E0"
                          SelectionChanged="CitiesDataGrid_SelectionChanged">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#2C3E50"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Padding" Value="10,8"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Naziv grada"
                                            Binding="{Binding Name}"
                                            Width="*"/>
                        <DataGridTextColumn Header="Država"
                                            Binding="{Binding Country.Name}"
                                            Width="150"/>
                        <DataGridTextColumn Header="Kod"
                                            Binding="{Binding Country.Code}"
                                            Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- Forma -->
            <Border Grid.Column="1"
                    BorderBrush="#CCCCCC"
                    BorderThickness="1"
                    Padding="20"
                    VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Detalji grada"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Naziv grada" Margin="0,0,0,5"/>
                    <TextBox x:Name="CityNameTextBox"
                             Padding="8"
                             FontSize="14"
                             Margin="0,0,0,15"/>

                    <TextBlock Text="Država" Margin="0,0,0,5"/>
                    <ComboBox x:Name="CountryComboBox"
                              Padding="8"
                              FontSize="14"
                              Margin="0,0,0,25"
                              DisplayMemberPath="Name"/>

                    <Button Content="Dodaj grad"
                            Click="AddButton_Click"
                            Padding="10,8"
                            FontSize="14"
                            Margin="0,0,0,10"
                            Background="#27AE60"
                            Foreground="White"/>

                    <Button Content="Izmeni grad"
                            Click="UpdateButton_Click"
                            Padding="10,8"
                            FontSize="14"
                            Margin="0,0,0,10"
                            Background="#2980B9"
                            Foreground="White"/>

                    <Button Content="Obrisi grad"
                            Click="DeleteButton_Click"
                            Padding="10,8"
                            FontSize="14"
                            Background="#E74C3C"
                            Foreground="White"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>