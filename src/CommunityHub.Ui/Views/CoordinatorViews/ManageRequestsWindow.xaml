<Window x:Class="CommunityHub.Ui.Views.CoordinatorViews.ManageRequestsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Requests" Height="600" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Requests" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Filter by Status:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="StatusFilterComboBox" Width="150" Padding="5"
                      SelectionChanged="StatusFilterComboBox_SelectionChanged"/>
            <TextBlock Text="Sort by Date:" VerticalAlignment="Center" Margin="15,0,10,0"/>
            <ComboBox x:Name="SortComboBox" Width="120" Padding="5"
                      SelectionChanged="SortComboBox_SelectionChanged"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <ItemsControl x:Name="RequestsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5"
                                Margin="0,0,0,10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="{Binding Citizen.Name, StringFormat='Citizen: {0}'}"
                                           FontWeight="Bold"/>
                                <TextBlock Text="{Binding Neighborhood.Name, StringFormat='District: {0}'}"/>
                                <TextBlock Text="{Binding CreatedAt, StringFormat='Date: {0:dd.MM.yyyy.}'}"/>
                                <TextBlock Text="{Binding Status, StringFormat='Status: {0}'}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button Content="Approve" Padding="10,5" Margin="0,0,10,0"
                                            Click="ApproveButton_Click" Tag="{Binding}"/>
                                    <Button Content="Decline" Padding="10,5"
                                            Click="RejectButton_Click" Tag="{Binding}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>