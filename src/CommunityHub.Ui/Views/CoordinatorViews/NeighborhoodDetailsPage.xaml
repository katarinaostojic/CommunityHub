<Page x:Class="CommunityHub.Ui.Views.CoordinatorViews.NeighborhoodDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Neighborhood Details">

    <StackPanel Margin="20">
        <TextBlock x:Name="NeighborhoodNameText" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Text="Members" FontSize="16" FontWeight="SemiBold" Margin="0,10,0,5"/>

        <ItemsControl ItemsSource="{Binding Members}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock Text="{Binding FullName}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Address}" Foreground="Gray"/>
                            <TextBlock Text="{Binding JoinedAt}" Foreground="Gray"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Content="Meetings" Width="150" HorizontalAlignment="Left" Margin="0,20,0,8"
        Click="MeetingsButton_Click"/>

        <Button Content="Back" Width="100" HorizontalAlignment="Left" Margin="0,0,0,0"
        Click="BackButton_Click"/>
        
        
    </StackPanel>
</Page>