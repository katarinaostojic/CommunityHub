<Window x:Class="CommunityHub.Ui.Views.CoordinatorViews.RejectReasonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reject Request" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundLightBrush}">
    <StackPanel Margin="20">

        <TextBlock Text="Rejection reason (optional):" Style="{StaticResource SectionTitle}" Margin="0,0,0,10"/>

        <TextBox x:Name="ReasonTextBox" Height="80" Padding="10,8"
                 TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,20"
                 BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Confirm" Click="ConfirmButton_Click"
                    Margin="0,0,8,0" Style="{StaticResource PrimaryButton}"/>
            <Button Content="Cancel" Click="CancelButton_Click"
                    Style="{StaticResource OutlineButton}"/>
        </StackPanel>

    </StackPanel>
</Window>