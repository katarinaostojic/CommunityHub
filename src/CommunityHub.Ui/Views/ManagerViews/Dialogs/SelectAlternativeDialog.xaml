<Window x:Class="CommunityHub.Ui.Views.ManagerViews.Dialogs.SelectAlternativeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Alternative"
        Width="400" Height="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F4F6F9">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select an alternative date range"
                   FontSize="18" FontWeight="Bold"
                   Foreground="#2C3E50" Margin="0,0,0,20"/>

        <ListBox Grid.Row="1" x:Name="AlternativesListBox"
                 Background="White" BorderBrush="#E0E0E0"
                 FontSize="13">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Padding="10,8"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Cancel"
                    Height="42" Margin="0,0,8,0"
                    Background="White" Foreground="#2C3E50"
                    BorderBrush="#E0E0E0" BorderThickness="1"
                    FontSize="13" Cursor="Hand"
                    Click="CancelButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Grid.Column="1" Content="Propose"
                    Height="42" Margin="8,0,0,0"
                    Background="#E67E22" Foreground="White"
                    BorderThickness="0" FontSize="13" FontWeight="SemiBold"
                    Cursor="Hand" Click="ProposeButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>