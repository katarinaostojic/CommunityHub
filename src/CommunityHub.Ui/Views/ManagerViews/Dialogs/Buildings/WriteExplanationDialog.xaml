<Window x:Class="CommunityHub.Ui.Views.ManagerViews.Dialogs.WriteExplanationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="520" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="White" CornerRadius="12" Padding="32,28,32,28">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" Opacity="0.2" ShadowDepth="4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,18">
                <TextBlock Text="Write an explanation"
                           FontSize="16" FontWeight="Bold"
                           Foreground="#2C3E50"
                           VerticalAlignment="Center"/>
                <Button Content="✕"
                        Width="32" Height="32"
                        FontSize="14"
                        Foreground="#888" Background="Transparent"
                        BorderThickness="0" Cursor="Hand"
                        HorizontalAlignment="Right"
                        Click="CancelButton_Click"/>
            </Grid>

            <!-- TextBox -->
            <TextBox Grid.Row="1" x:Name="ExplanationBox"
                     Height="110"
                     FontSize="13"
                     Padding="10,8"
                     BorderBrush="#CCCCCC"
                     BorderThickness="1"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,20">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <!-- Confirm dugme -->
            <Button Grid.Row="2" Content="Confirm"
                    Width="140" Height="40"
                    FontSize="13" FontWeight="SemiBold"
                    Foreground="White" Background="#2C3E50"
                    BorderThickness="0"
                    HorizontalAlignment="Right"
                    Cursor="Hand"
                    Click="ConfirmButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="12,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1a252f"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>