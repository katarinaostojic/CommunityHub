<Window x:Class="CommunityHub.Ui.Views.ManagerViews.Dialogs.ExplanationViewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="White" CornerRadius="12" Padding="36,32,36,28">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" Opacity="0.2" ShadowDepth="4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Rejection explanation"
                       FontSize="16" FontWeight="Bold"
                       Foreground="#2C3E50"
                       Margin="0,0,0,20"/>

            <Border Grid.Row="1"
                    BorderBrush="#E0E0E0" BorderThickness="1"
                    CornerRadius="8" Padding="16,12"
                    Background="#FAFAFA"
                    MinHeight="100"
                    Margin="0,0,0,24">
                <TextBlock x:Name="ExplanationText"
                           FontSize="13" Foreground="#444"
                           TextWrapping="Wrap"
                           LineHeight="22"/>
            </Border>

            <Button Grid.Row="2" Content="Back"
                    Width="120" Height="40"
                    FontSize="13"
                    Foreground="#2C3E50" Background="White"
                    BorderBrush="#CCCCCC" BorderThickness="1"
                    HorizontalAlignment="Right"
                    Cursor="Hand"
                    Click="BackButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="12,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>